import { SplitSizeColorDirective } from './split-size-color.directive';

describe('SplitSizeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SplitSizeColorDirective();
    expect(directive).toBeTruthy();
  });
});
